# Where enquiry submissions are POSTed as JSON.
# Static export inlines this at build time, so it must be set before `next build`.
# Cloudflare Pages Function, Vercel Route Handler, or any endpoint accepting POST JSON.
NEXT_PUBLIC_ENQUIRY_ENDPOINT=
